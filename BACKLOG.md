# loop-creator — fast-follow backlog

Code lives under `.claude/skills/loop-creator/`: harness `templates/orchestrator.py`,
adapter `adapters/markdown_adapter.py`. Tests go in `tests/` (importable via
`tests/conftest.py`). Each task must add pytest tests that pin the new behavior.

- [x] Add `stop` and `status` subcommands to `.claude/skills/loop-creator/templates/orchestrator.py`: `python orchestrator.py stop` creates `.loop/STOP`, `python orchestrator.py status` prints `.loop/report.md` (or a "no run yet" notice); add tests for a `cmd_stop`/`cmd_status` helper (spec §11).
- [x] Add stuck-detection to the harness: a pure `no_progress(prev_diff, cur_diff) -> bool` helper, and break the per-task retry loop early when an attempt's diff equals the previous attempt's diff (stop wasting budget); add tests for the helper (spec §8.1).
- [x] Add per-task model tiering to `orchestrator.py`: parse an optional trailing `[simple|complex|very-complex]` tag on a backlog task title, add `pick_model(task, config)` and `escalate(model, config)`, and use them in the run loop (escalate one tier on gate-failure retry); add tests for tier→model mapping and escalation (spec §8.4).
- [x] Add an anti-gaming diff-guard: `is_suppressing_diff(diff_text) -> bool` in `orchestrator.py` that flags a diff adding `skip`/`xfail`/`@ts-ignore`/`# type: ignore`/`# noqa`/`eslint-disable` or removing assertions; make the harness reject an otherwise-green attempt whose diff is suppressing unless the task title is tagged `[modifies-tests]`; add tests for the classifier on sample diffs (spec §8.5).
- [x] Wire the §8.7 mechanical oversize-guard into `.claude/skills/loop-creator/templates/orchestrator.py`: add a pure `is_oversize_diff(diff_text, threshold) -> bool` that counts the distinct changed files in a unified diff (from the `diff --git`/`+++` file headers, never the `+`/`-` content lines) and returns True when the count exceeds `threshold`; add an `oversize_file_threshold` config key (default 25) read via `cfg.get`; and in `process_task`, when a task is about to be blocked (retries exhausted / no-progress), if the last attempt's diff is oversize, set the block reason to `"likely too large — split needed"` instead of the bare gate reason; add tests for the helper covering under/at/over threshold and header-vs-content counting (spec §8.7).
- [x] Add a §16 rolling `.loop/learned.md` writer to `.claude/skills/loop-creator/templates/orchestrator.py`: pure `format_learned_bullet(text) -> str` (one `- `-prefixed, whitespace-collapsed line) and pure `dedupe_bounded(bullets, limit=15) -> list` (drop exact-duplicate bullets keeping the most-recent occurrence, then cap to the last `limit`), plus `append_learned(path, bullet)` that reads any existing bullets, appends the new one, applies `dedupe_bounded`, and rewrites the file; add tests for both pure helpers (dedup keeps latest, bound caps to limit) (spec §16).

## dogfood #3 — §9 budget guardrails + §8.6 acceptance (parse half)

- [x] Add a §9 dollar budget cap to the harness (`.claude/skills/loop-creator/templates/orchestrator.py`): a pure `over_budget(cost_usd, cap) -> bool` returning True only when `cap` is a positive number and `cost_usd >= cap` (a None / 0 / negative cap means no cap, so always False); read `cap = cfg.get("budget", {}).get("max_cost_usd")`; in `main`'s while-loop at the task boundary (before `pick_task`), set `halt` to a clear `budget cap reached ($<spent> / $<cap>)` message and `break` when `over_budget(state["cost_usd"], cap)` — halt only between tasks, never mid-task (respect the atomicity invariant); add `tests/test_budget_cap.py` pinning the under / at / over cap cases plus the disabled (None, 0, negative) cases. [complex]
- [x] Add a §9 wall-clock budget cap to the harness (`.claude/skills/loop-creator/templates/orchestrator.py`): a pure `over_wallclock(started_epoch, now_epoch, max_minutes) -> bool` returning True only when `max_minutes` is a positive number, `started_epoch` is truthy, and `now_epoch - started_epoch >= max_minutes * 60` (a None / 0 / negative `max_minutes` or a missing/None `started_epoch` means no cap, so always False); record `started_epoch` via `time.time()` in the initial `state` dict alongside `started_at`, and read it with `state.get("started_epoch")` so an older state without it degrades to no-cap; read `cfg.get("budget", {}).get("max_wallclock_min")`; in `main`'s task-boundary loop (before `pick_task`), `break` with a clear `wall-clock cap reached (<elapsed>m / <cap>m)` halt when over; add `tests/test_wallclock_cap.py` covering under / at / over, disabled, and missing-start. [complex]
- [x] Add §8.6 acceptance-criterion parsing + prompt surfacing (parse-and-surface half only — harness enforcement of the acceptance test is a separate later task). Grammar: a backlog checkbox title MAY carry a trailing acceptance marker `@acceptance(<criterion text>)`. Add a pure `parse_acceptance(title) -> (clean_title, acceptance_or_None)` in `.claude/skills/loop-creator/adapters/markdown_adapter.py` that removes an `@acceptance(...)` marker (non-greedy `[^)]*` body) from anywhere in the title, returns the criterion string (or None when absent) and the title with the marker removed and internal whitespace collapsed; give the `Task` dataclass an `acceptance: str | None = None` field and populate it in `_items` (set `title` to the cleaned remainder so any trailing `[tier]` tag stays at the end and `parse_tier` keeps matching); in `orchestrator.py` `build_prompt`, read the field with `getattr(task, "acceptance", None)` (so existing callers passing a bare task still work) and, when set, append a hard rule line stating the acceptance criterion the work must satisfy and that a test pinning it must be present. Add tests: `tests/test_acceptance_parse.py` for the parser (marker present / absent, marker alongside a trailing `[complex]` tier tag, whitespace collapse) and a check that `build_prompt` surfaces the criterion when the task carries one. [complex]

## dogfood #4 — §8.6 acceptance enforcement + §9 projection + protected-paths guard

- [x] Add §8.6 acceptance-test enforcement to `.claude/skills/loop-creator/templates/orchestrator.py` (builds on the shipped parse-half — tasks already carry `task.acceptance`). Add a pure `acceptance_tokens(criterion) -> list` that extracts test-identifying tokens from an acceptance-criterion string: pytest node ids / function names matching `test_\w+`, and file paths ending in `.py`. Add a pure `acceptance_in_diff(diff_text, criterion) -> bool` returning True when at least one token from `acceptance_tokens(criterion)` appears on an ADDED line of the diff (a line starting with a single `+`, never `-`/context/`+++` headers). Wire into `process_task`'s done-path: after the gate passes and the existing suppressing-diff check, when `getattr(task, "acceptance", None)` is set AND `acceptance_tokens(task.acceptance)` is non-empty AND `not acceptance_in_diff(cur_diff, task.acceptance)`, REJECT the attempt (do not mark done) by setting `prior` to a message telling the model to add the acceptance test that pins the criterion — mirror the existing suppressing-diff rejection branch exactly (same control-flow shape). A criterion with no extractable tokens is advisory-only (not enforced, falls through to success). Add `tests/test_acceptance_enforce.py` covering token extraction, `acceptance_in_diff` true/false, the no-token skip case, and that a green-but-missing-acceptance-test attempt is rejected. [complex]
- [x] Add a §9 pre-task budget PROJECTION halt to `.claude/skills/loop-creator/templates/orchestrator.py`, refining the existing post-hoc dollar cap (`over_budget`). Add a pure `avg_task_cost(cost_usd, tasks_done) -> float` (return `0.0` when `tasks_done <= 0`, else `cost_usd / tasks_done`) and a pure `projected_over_budget(cost_so_far, avg_cost, cap) -> bool` (True only when `cap` is a positive number and `cost_so_far + avg_cost > cap`; a None / 0 / negative cap means no cap, so always False). In `main`'s task-boundary loop, right after the existing `over_budget` check, compute `done_count = sum(1 for r in state["results"].values() if r.get("status") == "done")`, `avg = avg_task_cost(state["cost_usd"], done_count)`, and if `projected_over_budget(state["cost_usd"], avg, cap)` set `halt` to a clear `projected budget cap reached ($<spent> + ~$<avg>/task > $<cap>)` message and `break` BEFORE starting the next task (§9: never start a task whose projected cost would breach the cap). On the first task `done_count` is 0 so `avg` is 0 and the projection reduces to the plain `over_budget` case (already handled) — no spurious early halt. Add `tests/test_budget_projection.py` for both helpers (avg zero-guard and normal average; projection under / at / over the cap; the disabled None/0/negative-cap cases). [complex]
- [ ] Add a §9 protected-paths post-commit guard to `.claude/skills/loop-creator/templates/orchestrator.py`. Add a pure `touches_protected(diff_text, patterns) -> bool` that returns True when any file changed in the diff matches any glob in `patterns`: get the changed files via the existing `_changed_files(diff_text)` helper (file headers only, never `+`/`-` content), and match each against each pattern with `fnmatch.fnmatch`, treating a pattern ending in `/**` as a recursive prefix (a path is protected when it equals the prefix or starts with `prefix + "/"`). Empty `patterns` never matches. Read `patterns = cfg.get("guardrails", {}).get("protected_paths", [])`. Wire into `process_task`'s done-path: after the gate passes (alongside the suppressing / acceptance rejections), when the committed diff `touches_protected(cur_diff, patterns)`, REJECT the attempt (do not mark done) by setting `prior` to a message naming the protected paths this task must not modify — mirror the suppressing-diff rejection branch. Add `tests/test_protected_paths.py` covering a plain glob (`.env*`), a `secrets/**` recursive match, a clearly non-matching diff, and empty patterns (never blocks). [complex]
