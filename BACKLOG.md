# nocturne — fast-follow backlog

Code lives under `.claude/skills/nocturne/`: harness `templates/orchestrator.py`,
adapter `adapters/markdown_adapter.py`. Tests go in `tests/` (importable via
`tests/conftest.py`). Each task must add pytest tests that pin the new behavior.

- [x] Add `stop` and `status` subcommands to `.claude/skills/nocturne/templates/orchestrator.py`: `python orchestrator.py stop` creates `.loop/STOP`, `python orchestrator.py status` prints `.loop/report.md` (or a "no run yet" notice); add tests for a `cmd_stop`/`cmd_status` helper (spec §11).
- [x] Add stuck-detection to the harness: a pure `no_progress(prev_diff, cur_diff) -> bool` helper, and break the per-task retry loop early when an attempt's diff equals the previous attempt's diff (stop wasting budget); add tests for the helper (spec §8.1).
- [x] Add per-task model tiering to `orchestrator.py`: parse an optional trailing `[simple|complex|very-complex]` tag on a backlog task title, add `pick_model(task, config)` and `escalate(model, config)`, and use them in the run loop (escalate one tier on gate-failure retry); add tests for tier→model mapping and escalation (spec §8.4).
- [x] Add an anti-gaming diff-guard: `is_suppressing_diff(diff_text) -> bool` in `orchestrator.py` that flags a diff adding `skip`/`xfail`/`@ts-ignore`/`# type: ignore`/`# noqa`/`eslint-disable` or removing assertions; make the harness reject an otherwise-green attempt whose diff is suppressing unless the task title is tagged `[modifies-tests]`; add tests for the classifier on sample diffs (spec §8.5).
- [x] Wire the §8.7 mechanical oversize-guard into `.claude/skills/nocturne/templates/orchestrator.py`: add a pure `is_oversize_diff(diff_text, threshold) -> bool` that counts the distinct changed files in a unified diff (from the `diff --git`/`+++` file headers, never the `+`/`-` content lines) and returns True when the count exceeds `threshold`; add an `oversize_file_threshold` config key (default 25) read via `cfg.get`; and in `process_task`, when a task is about to be blocked (retries exhausted / no-progress), if the last attempt's diff is oversize, set the block reason to `"likely too large — split needed"` instead of the bare gate reason; add tests for the helper covering under/at/over threshold and header-vs-content counting (spec §8.7).
- [x] Add a §16 rolling `.loop/learned.md` writer to `.claude/skills/nocturne/templates/orchestrator.py`: pure `format_learned_bullet(text) -> str` (one `- `-prefixed, whitespace-collapsed line) and pure `dedupe_bounded(bullets, limit=15) -> list` (drop exact-duplicate bullets keeping the most-recent occurrence, then cap to the last `limit`), plus `append_learned(path, bullet)` that reads any existing bullets, appends the new one, applies `dedupe_bounded`, and rewrites the file; add tests for both pure helpers (dedup keeps latest, bound caps to limit) (spec §16).

## dogfood #3 — §9 budget guardrails + §8.6 acceptance (parse half)

- [x] Add a §9 dollar budget cap to the harness (`.claude/skills/nocturne/templates/orchestrator.py`): a pure `over_budget(cost_usd, cap) -> bool` returning True only when `cap` is a positive number and `cost_usd >= cap` (a None / 0 / negative cap means no cap, so always False); read `cap = cfg.get("budget", {}).get("max_cost_usd")`; in `main`'s while-loop at the task boundary (before `pick_task`), set `halt` to a clear `budget cap reached ($<spent> / $<cap>)` message and `break` when `over_budget(state["cost_usd"], cap)` — halt only between tasks, never mid-task (respect the atomicity invariant); add `tests/test_budget_cap.py` pinning the under / at / over cap cases plus the disabled (None, 0, negative) cases. [complex]
- [x] Add a §9 wall-clock budget cap to the harness (`.claude/skills/nocturne/templates/orchestrator.py`): a pure `over_wallclock(started_epoch, now_epoch, max_minutes) -> bool` returning True only when `max_minutes` is a positive number, `started_epoch` is truthy, and `now_epoch - started_epoch >= max_minutes * 60` (a None / 0 / negative `max_minutes` or a missing/None `started_epoch` means no cap, so always False); record `started_epoch` via `time.time()` in the initial `state` dict alongside `started_at`, and read it with `state.get("started_epoch")` so an older state without it degrades to no-cap; read `cfg.get("budget", {}).get("max_wallclock_min")`; in `main`'s task-boundary loop (before `pick_task`), `break` with a clear `wall-clock cap reached (<elapsed>m / <cap>m)` halt when over; add `tests/test_wallclock_cap.py` covering under / at / over, disabled, and missing-start. [complex]
- [x] Add §8.6 acceptance-criterion parsing + prompt surfacing (parse-and-surface half only — harness enforcement of the acceptance test is a separate later task). Grammar: a backlog checkbox title MAY carry a trailing acceptance marker `@acceptance(<criterion text>)`. Add a pure `parse_acceptance(title) -> (clean_title, acceptance_or_None)` in `.claude/skills/nocturne/adapters/markdown_adapter.py` that removes an `@acceptance(...)` marker (non-greedy `[^)]*` body) from anywhere in the title, returns the criterion string (or None when absent) and the title with the marker removed and internal whitespace collapsed; give the `Task` dataclass an `acceptance: str | None = None` field and populate it in `_items` (set `title` to the cleaned remainder so any trailing `[tier]` tag stays at the end and `parse_tier` keeps matching); in `orchestrator.py` `build_prompt`, read the field with `getattr(task, "acceptance", None)` (so existing callers passing a bare task still work) and, when set, append a hard rule line stating the acceptance criterion the work must satisfy and that a test pinning it must be present. Add tests: `tests/test_acceptance_parse.py` for the parser (marker present / absent, marker alongside a trailing `[complex]` tier tag, whitespace collapse) and a check that `build_prompt` surfaces the criterion when the task carries one. [complex]

## dogfood #4 — §8.6 acceptance enforcement + §9 projection + protected-paths guard

- [x] Add §8.6 acceptance-test enforcement to `.claude/skills/nocturne/templates/orchestrator.py` (builds on the shipped parse-half — tasks already carry `task.acceptance`). Add a pure `acceptance_tokens(criterion) -> list` that extracts test-identifying tokens from an acceptance-criterion string: pytest node ids / function names matching `test_\w+`, and file paths ending in `.py`. Add a pure `acceptance_in_diff(diff_text, criterion) -> bool` returning True when at least one token from `acceptance_tokens(criterion)` appears on an ADDED line of the diff (a line starting with a single `+`, never `-`/context/`+++` headers). Wire into `process_task`'s done-path: after the gate passes and the existing suppressing-diff check, when `getattr(task, "acceptance", None)` is set AND `acceptance_tokens(task.acceptance)` is non-empty AND `not acceptance_in_diff(cur_diff, task.acceptance)`, REJECT the attempt (do not mark done) by setting `prior` to a message telling the model to add the acceptance test that pins the criterion — mirror the existing suppressing-diff rejection branch exactly (same control-flow shape). A criterion with no extractable tokens is advisory-only (not enforced, falls through to success). Add `tests/test_acceptance_enforce.py` covering token extraction, `acceptance_in_diff` true/false, the no-token skip case, and that a green-but-missing-acceptance-test attempt is rejected. [complex]
- [x] Add a §9 pre-task budget PROJECTION halt to `.claude/skills/nocturne/templates/orchestrator.py`, refining the existing post-hoc dollar cap (`over_budget`). Add a pure `avg_task_cost(cost_usd, tasks_done) -> float` (return `0.0` when `tasks_done <= 0`, else `cost_usd / tasks_done`) and a pure `projected_over_budget(cost_so_far, avg_cost, cap) -> bool` (True only when `cap` is a positive number and `cost_so_far + avg_cost > cap`; a None / 0 / negative cap means no cap, so always False). In `main`'s task-boundary loop, right after the existing `over_budget` check, compute `done_count = sum(1 for r in state["results"].values() if r.get("status") == "done")`, `avg = avg_task_cost(state["cost_usd"], done_count)`, and if `projected_over_budget(state["cost_usd"], avg, cap)` set `halt` to a clear `projected budget cap reached ($<spent> + ~$<avg>/task > $<cap>)` message and `break` BEFORE starting the next task (§9: never start a task whose projected cost would breach the cap). On the first task `done_count` is 0 so `avg` is 0 and the projection reduces to the plain `over_budget` case (already handled) — no spurious early halt. Add `tests/test_budget_projection.py` for both helpers (avg zero-guard and normal average; projection under / at / over the cap; the disabled None/0/negative-cap cases). [complex]
- [x] Add a §9 protected-paths post-commit guard to `.claude/skills/nocturne/templates/orchestrator.py`. Add a pure `touches_protected(diff_text, patterns) -> bool` that returns True when any file changed in the diff matches any glob in `patterns`: get the changed files via the existing `_changed_files(diff_text)` helper (file headers only, never `+`/`-` content), and match each against each pattern with `fnmatch.fnmatch`, treating a pattern ending in `/**` as a recursive prefix (a path is protected when it equals the prefix or starts with `prefix + "/"`). Empty `patterns` never matches. Read `patterns = cfg.get("guardrails", {}).get("protected_paths", [])`. Wire into `process_task`'s done-path: after the gate passes (alongside the suppressing / acceptance rejections), when the committed diff `touches_protected(cur_diff, patterns)`, REJECT the attempt (do not mark done) by setting `prior` to a message naming the protected paths this task must not modify — mirror the suppressing-diff rejection branch. Add `tests/test_protected_paths.py` covering a plain glob (`.env*`), a `secrets/**` recursive match, a clearly non-matching diff, and empty patterns (never blocks). [complex]

## dogfood #5 — §8.3 units + §9 checkpoint modes + §8.6 non-codifiable routing

- [x] Add §8.3 unit grouping to `.claude/skills/nocturne/adapters/markdown_adapter.py`. A `## <heading>` line opens a UNIT; every checkbox task belongs to the most-recent preceding `##` heading (tasks before any heading belong to a default unit named `""`). Add a pure `parse_units(lines) -> list` that returns, in document order, `(unit_title, [task_index, ...])` tuples — include only units containing at least one checkbox, and `task_index` is the 0-based checkbox index matching `Task.index`. Give the `Task` dataclass a `unit: str = ""` field and populate it in `_items` (track the current `##` heading text as you scan the lines). Add a `MarkdownBacklog.units()` method returning `parse_units(self._read())`. Keep all existing behavior intact (title / `@acceptance(...)` / `[tier]` parsing unchanged). Add `tests/test_units.py` covering: tasks under headings grouped correctly, pre-heading tasks in the `""` unit, a heading with no checkboxes omitted from the result, and `Task.unit` populated on parsed tasks. [complex]
- [x] Add §9 checkpoint modes to `.claude/skills/nocturne/templates/orchestrator.py` (config `mode`: `auto` (default) | `checkpoint-task` | `checkpoint-unit`). Add a pure `should_checkpoint(mode, current_task, next_task) -> bool`: return False when `mode == "auto"` or `next_task is None` (nothing left to pause before); return True when `mode == "checkpoint-task"`; for `mode == "checkpoint-unit"` return True only when `getattr(next_task, "unit", "") != getattr(current_task, "unit", "")` (a unit boundary). Wire into `main`: after a task completes DONE and `save_state`/`write_report` have run, compute `nxt = pick_task(adapter, state)` and if `should_checkpoint(cfg.get("mode", "auto"), task, nxt)`, set `halt` to a clear `checkpoint (<mode>): task <id> done — resume with 'python .loop/orchestrator.py' to continue` message and `break` (clean + resumable; the next run resumes at the next todo). Only checkpoint after a DONE task, never after a blocked one. Add `tests/test_checkpoint.py` covering `auto` (never), `checkpoint-task` (True, but False when next is None), and `checkpoint-unit` (True only across a unit boundary, False within a unit). [complex]
- [x] Add §8.6 non-codifiable-acceptance routing to `.claude/skills/nocturne/templates/orchestrator.py`. Add a pure `is_codifiable_acceptance(criterion) -> bool`: True when `acceptance_tokens(criterion)` is non-empty (the criterion names a test) OR the criterion contains a runnable-command signal matched by a crisp regex over known runners/verbs (`pytest`, `python`, `npm`, `pnpm`, `ruff`, `make`, a leading `./`, a backtick-wrapped command, or the phrases `exit 0` / `returns 0` / `passes`); False for empty / None or purely subjective prose. Wire into `main`: after `pick_task` returns a task, when `getattr(task, "acceptance", None)` is set AND NOT `is_codifiable_acceptance(task.acceptance)`, DO NOT run the task — mark it blocked in `state["results"]` with reason `acceptance not codifiable — needs a human checkpoint` (mirror the existing blocked-write shape), print `[loop] CHECKPOINT <id>`, increment `state["iterations"]` and `save_state`/`write_report`, but DO NOT touch `consecutive_failures` (this is a routing decision, not a task failure), then `continue` the loop (spec §8.6: auto-mode refuses non-codifiable acceptance and continues with the rest of the backlog rather than guessing). This is distinct from the acceptance ENFORCEMENT branch, which fires only for codifiable, test-named criteria. Factor the pick-time decision into a small `needs_human_checkpoint(task) -> bool` helper so it is unit-testable. Add `tests/test_codifiable.py` covering: a test-named criterion (codifiable), a runnable-command criterion (codifiable), subjective prose (non-codifiable), empty / None (non-codifiable), and that `needs_human_checkpoint` is True for a task with non-codifiable acceptance and False for one with codifiable or no acceptance. [complex]

## dogfood #6 — usability + robustness (from the symphony external run)

Findings from nocturne's first external-repo run (symphony, fable). Loop-workable
tasks (pure core + pytest teeth) are checkboxes; design-layer findings are noted after.
Keep these UNTAGGED (no `[tier]`) if running with fable — a `[complex]` tag maps to opus
in `pick_model` and overrides `config.model`.

- [x] Add a baseline-green preflight to `.claude/skills/nocturne/templates/orchestrator.py` so the loop never mislabels pre-existing gate failures as task failures (symphony had a red `ruff` baseline). Add a pure `baseline_halt_reason(ok, tail, require_green) -> str | None` that returns a clear halt message (naming that the baseline gate is red and to fix the repo or adjust the gate, plus a short tail excerpt) when `require_green` is true and `ok` is false, else None. Read `require_green = cfg.get("require_green_baseline", True)`. In `main`, after branch setup and before the while-loop, run `run_gate(cfg["gate"])` once; if `baseline_halt_reason(...)` returns a message, set `halt` and skip the loop (no task churn). Add `tests/test_baseline_gate.py` covering: red baseline + require-true -> message, green baseline -> None, red baseline + require-false (opt-out) -> None.

- [x] Stop the first task's `--amend` from folding an uncommitted backlog into its commit (`.claude/skills/nocturne/templates/orchestrator.py`). Add a pure `needs_backlog_seed(porcelain_paths, backlog_rel) -> bool` returning True when the backlog path appears among the parsed `git status --porcelain` paths (reuse the existing `_porcelain_paths`). In `main`, right after `ensure_branch` and before the task loop, when `needs_backlog_seed(...)` is true, commit only the backlog file on the loop branch as a `chore: seed nocturne backlog` commit so later task commits carry only their own checkbox flip. Add `tests/test_backlog_seed.py` covering: untracked backlog -> True, clean/committed backlog -> False, an unrelated dirty path -> False.

### design-layer findings (by hand — weak/no pytest teeth; do NOT feed to the autonomous loop)

- SHIPPED `644cd04` — Gate derivation must weight an explicit "Done = <cmd>" statement in `CLAUDE.md`/`AGENTS.md` ABOVE language-default guessing. Symphony's real bar was `python -m pytest tests/unit/`; a naive `ruff && pytest` would false-block every task (ruff was pre-existing-red and not their gate). Update `reference/gate-derivation.md` precedence + the SKILL recon step.
- SHIPPED `0741493` — Auto-detect a non-checkbox / prose `BACKLOG.md` and route it to the SPEC-groom path; also write the groomed checkbox backlog to a SEPARATE file when the repo's own backlog uses a different convention (symphony: prose + remove-when-done). Strengthen SKILL.md step 2 + `reference/spec-to-backlog.md`.
- SHIPPED `dc8181a` — Add a design-layer ticket-sanity pass before launch: verify each groomed task is a REAL, non-redundant change. Symphony: two agent-proposed "bugs" were intended behavior / already handled at another layer. Trust-but-verify the BACKLOG, not just task output.
- Cosmetic: loop commits self-attribute `Co-Authored-By: Opus 4.7` even on a fable run (the headless model's own commit text); not nocturne-controllable.

## dogfood #7 — resume UX + timeout observability

Groomed 2026-07-05 from what's still open after the by-hand pass. Loop-workable
slices only (pure core + pytest teeth). Keep these UNTAGGED (no `[tier]`) — a tier
tag overrides `config.model` in `pick_model`. Hand-build items listed after.

- [x] Reset the consecutive-failure window on resume in `.claude/skills/nocturne/templates/orchestrator.py` (kills the accepted v1 wart: resuming after a `max_consecutive_failures` halt re-halts immediately because the counter persists in state.json). Add a pure `resumed_failure_count(consecutive_failures, max_consecutive_failures) -> int` that returns 0 when `max_consecutive_failures` is a positive number AND `consecutive_failures >= max_consecutive_failures` (a human deliberately relaunching after a failure halt gets a fresh failure window), and returns `consecutive_failures` unchanged otherwise (including when the cap is None / 0 / negative). In `main`, on the resume path only (when `load_state()` returned an existing state, not the fresh-state branch), apply `state["consecutive_failures"] = resumed_failure_count(state["consecutive_failures"], cfg.get("budget", {}).get("max_consecutive_failures", 3))` before the while-loop so the halt check sees the reset value. Add `tests/test_resume_failures.py` covering: at-cap -> 0, above-cap -> 0, below-cap unchanged, zero unchanged, and None / 0 / negative cap -> unchanged.
- [x] Add an `unblock` subcommand to `.claude/skills/nocturne/templates/orchestrator.py` so blocked tasks can be retried without hand-editing or deleting `.loop/state.json` (today `pick_task` skips blocked entries forever). Add a pure `clear_blocked(results, task_id=None) -> dict` returning a NEW dict (input never mutated): with `task_id=None`, every entry whose `status == "blocked"` is removed; with a task_id, only that entry is removed and only when its status is "blocked" (a done entry or an unknown id leaves the result equal to the input). Add `cmd_unblock(task_id=None)` that prints a notice and returns 1 when `.loop/state.json` is missing, else loads it, applies `clear_blocked` to `state["results"]`, prints how many entries were cleared, saves via the existing `save_state`, and returns 0. Extend the `__main__` argv dispatch with `unblock` taking an optional task-id argument (`python .loop/orchestrator.py unblock [task-id]`), and add it to the usage string on the unknown-command branch. Add `tests/test_unblock.py` covering: all blocked cleared while done entries survive, a single id cleared, a done id NOT cleared even when named, an unknown id no-op, and input-dict non-mutation.
- [x] Surface per-task timeout kills as an actionable retry message in `.claude/skills/nocturne/templates/orchestrator.py` (today a watchdog kill usually surfaces as the generic "No commit was produced", hiding the real cause). In `run_claude`, record when the watchdog Timer actually fired: have the timer callback append to a local `killed` list before calling `proc.kill()`. Extend `ClaudeResult` to `collections.namedtuple("ClaudeResult", "cost rate_limited resets_at timed_out", defaults=[False])` so existing 3-field constructions keep working, and return `timed_out=bool(killed)`. Add a pure `timeout_prior(timed_out, seconds) -> str | None` returning None when `timed_out` is falsy, else a one-line message stating the attempt was killed at the per-task cap, naming the seconds value and the `budget.max_seconds_per_task` config key, and advising to raise the cap or split the task. In `process_task`, in the no-new-commit branch, set `prior = timeout_prior(result.timed_out, cfg.get("budget", {}).get("max_seconds_per_task", 1800)) or "No commit was produced. You must commit your work."`. Add `tests/test_timeout_reason.py` covering: `timeout_prior(True, 1800)` names both the seconds and the config key, `timeout_prior(False, 1800)` is None, and a 3-arg `ClaudeResult(...)` has `timed_out` False (back-compat).

### dogfood #7 — not in this batch (by hand / not taskable)

- §11 `--detach` lifecycle: OS-spawn behavior, not gate-catchable — hand-build.
- §8.3 PR consumer (push + `gh pr create --draft` per unit): outward-facing + weak pytest teeth — hand-build; the pure render half (unit -> PR title/body) could become a later loop task.
- §17 github/gitlab adapters: network-backed — needs an injected-transport design first.
- §9 live `status=="rejected"` 429 event: observational — watch the next naturally-limited run and capture the raw event.

### analysis session 2026-07-05 — by-hand improvement pass (audit trail)

Four read-only lenses (usability / optimization / bugs / docs) fanned out over the
skill; every finding re-verified against the code before acting. Shipped beyond the
two checkboxes above: atomic `state.json` writes (`4a97b75` — torn state lost the
branch pin on resume), null/garbage stream-json value hardening (`c47564c`), config
schema completeness in SKILL.md + SPEC §13 v1-naming note (`b2a5c6d`), stale
"out of v1" scope list (`6addcac`).

Rejected findings (one line each — the kill record):

- All 12 optimization findings: micro-CPU (event-list rewalks, backlog re-parse, gate-string rebuild, done_count recount) — run cost is model tokens + gate runtime, not python loops; caching adds staleness risk for zero felt latency.
- "Trim acceptance/prior from retry prompts": wrong — every attempt is a fresh `claude -p` process; dropping them would starve the retry of its failure context.
- "Gate feed decode when live_feed off": decode feeds `activity.log` regardless; only the print is gated.
- Lock-file encoding gap: PID is ASCII digits — byte-identical under any Windows codepage.
- `parse_events` silently drops malformed lines: tolerable by design; worst case under-counts pennies of cost on a killed stream.
- Amend-failure handling: hooks already passed on the model's original commit, so an amend re-run failing is rare; the crash path recovers on resume, and a "fix" risks discarding a good commit.
- Backslash paths in `touches_protected`: git diff always emits forward slashes.
- SKILL step-1/2 reordering, spec-sync "offer" ambiguity, missing gate/checkpoint/resume examples: already present or already addressed to the right audience (SKILL.md speaks to the design-layer Claude).
- SPEC §8.3 "documents unbuilt PRs": SPEC is the design target; built-vs-not is tracked here and in memory.

## dogfood #8 — multi-run observability: global registry + status surfaces

Groomed 2026-07-05 after dogfood #7. User steer: NO browser tab, NO Windows
toasts, NO tray — the surfaces are the Claude Code statusline (ambient pull)
and a Monitor-armed Claude session (push into chat), plus a plain-shell watch
fallback. Write side lives in the orchestrator; read side lives in a NEW
standalone template module so statusline/watch consumers run outside any repo.
Keep these UNTAGGED (no tier tag) — a tier tag overrides config.model in
pick_model. Hand-build items listed after.

- [x] Add a global run-registry WRITE side to `.claude/skills/nocturne/templates/orchestrator.py` so concurrent loop runs are discoverable outside their repos. Add `registry_dir(env=None)` returning `Path((env or os.environ).get("NOCTURNE_HOME") or (Path.home() / ".nocturne"))` (the env override makes tests hermetic — tests MUST set NOCTURNE_HOME to a tmp dir, never touch the real home). Add a pure `run_id(root_name, branch)` that takes the repo dir name and the loop branch, keeps the branch part after the last `/`, joins as `f"{root_name}-{tail}"`, and replaces every char outside A-Za-z0-9 dash underscore with a dash. Add a pure `heartbeat_record(run_id, root, branch, pid, status, task_id, task_title, attempt, model, cost_usd, done, blocked, todo, now)` returning a dict with exactly those keys plus `updated_at` (epoch float = now) — `task_title` truncated to 80 chars, `task_id`/`task_title`/`attempt`/`model` may be None between tasks. Add `write_heartbeat(path, record)` that creates parent dirs and reuses the existing `_atomic_write_text` with `json.dumps`. Wire into the run: the heartbeat file is `registry_dir() / "runs" / f"{run_id}.json"`; write it (status "running", current task/attempt/model) at each attempt start, after each task result (done/blocked counts from `state["results"]`, todo = unchecked count from the adapter), with status "paused" when a rate-limit pause begins, and a final write in the halt path with status "done" when the backlog is empty else "halted" (task fields None). Registry write failures must never crash the loop — wrap the IO call in try/except and continue. Add `tests/test_registry.py`: run_id sanitization (slash and space become dashes), heartbeat_record key completeness + title truncation + None-task shape, write_heartbeat produces parseable json under a NOCTURNE_HOME tmp override, and registry_dir honors the override.
- [x] Create `.claude/skills/nocturne/templates/nocturne_status.py` — a NEW standalone read-side status module (stdlib only, no orchestrator import; `tests/conftest.py` already puts the templates dir on sys.path so tests import it directly). `load_records(runs_dir)` returns a list of dicts parsed from `*.json` in the dir, silently dropping unreadable or invalid files (a torn write must not break the viewer); a missing dir returns an empty list. Pure `classify_run(record, now, stale_after_s=900)` returns one of "running" "paused" "halted" "done" "stale": terminal statuses ("halted"/"done") pass through unchanged; "running"/"paused" whose `now - updated_at` exceeds stale_after_s become "stale" (a crashed loop shows up instead of lying "running" forever). Pure `status_line(records, now)` returns ONE line for the Claude Code statusline: `nocturne: idle` for an empty list, else `nocturne: <n> running` followed by up to 3 active-run segments (most recently updated first) of the form `<run_id> <task_id> a<attempt> <model with any leading "claude-" stripped> $<cost_usd to 2dp>`, then `+<n> more` when over 3, then counts of paused and stale runs when nonzero, all joined with ` · `. Pure `watch_rows(records, now)` returns one aligned text row per record (all records, most recent first): a status glyph (▶ running, ⏳ paused, ■ done, ✖ halted, ? stale), run_id, task_id + attempt (or `-` between tasks), model, cost to 2dp, `d/b/t` counts, and a compact age like `12s` / `3m` / `2h` from `now - updated_at`. `__main__` argv: `--line` prints `status_line(load_records(...), time.time())` once; `--watch [seconds]` reprints watch_rows every interval (default 2.0) until KeyboardInterrupt; no args prints watch_rows once. Add `tests/test_status_module.py`: the classification matrix (fresh running stays running, old running goes stale, done passes through), status_line for empty / one / four active runs (cap + `+1 more`), watch_rows glyphs + age buckets, and load_records dropping a corrupt file while keeping a valid one.
- [x] Add a global append-only EVENT feed to `.claude/skills/nocturne/templates/orchestrator.py` for push consumers (a Claude session arms a file-watcher on the feed and narrates new lines into chat). Pure `event_line(now_iso, repo_name, task_id, event, detail="")` returns `"<now_iso> <repo_name> <task_id> <EVENT>"` plus ` <detail>` when detail is non-empty — internal whitespace in detail collapsed to single spaces, the whole line capped at 200 chars, no trailing newline. `append_event(path, line)` creates parent dirs and appends `line + "\n"` utf-8 (append-only; rotation is explicitly out of scope for v1). The feed lives at `registry_dir() / "events.log"`. Wire into `main` at boundaries only (never mid-attempt): TASK_DONE (detail: short sha + cost), TASK_BLOCKED (detail: reason), PAUSED (detail: resets epoch), HALT (detail: the halt message), RUN_DONE when the backlog is empty. Feed write failures must never crash the loop (try/except, continue). Add `tests/test_event_feed.py`: line format with and without detail, whitespace collapse, the 200-char cap, and append_event creating the file then appending a second line under a NOCTURNE_HOME tmp override.

### dogfood #8 — not in this batch (ALL SHIPPED by hand 2026-07-05)

- [x] Statusline hookup — user settings.json statusLine now chains the caveman
  statusline + `nocturne_status.py --line` in one command (refreshInterval 30;
  `-ExecutionPolicy Bypass` dropped — CurrentUser RemoteSigned suffices).
- [x] `/nocturne watch` SKILL.md step (`b54191c`) — Monitor on
  `~/.nocturne/events.log`, proven live (RUN_DONE landed in chat).
- [x] §11 detach lifecycle (`b57cfba`) — `detach` subcommand, full-lifecycle
  smoke green (spawn -> baseline -> RUN_DONE event -> final "done" heartbeat).
- Follow-up candidate (dogfood #9): run-start heartbeat — today the first
  registry write happens at the first attempt (or halt), so the branch-setup /
  baseline-gate phase renders `idle`. Also: spawn the detached child with
  `python -u` so detached.log streams instead of flushing at exit.
- Rejected surfaces (user): browser dashboard tab, Windows toasts, tray icon.

## public-launch #1 — repo goes public

Groomed 2026-07-05 from a prod-readiness scan. The repo will be made PUBLIC on
GitHub shortly after this batch. Docs-only tasks: the quality gate must stay
green, but no new tests are expected for license/readme/comment changes.
Keep these UNTAGGED (no tier tag) — a tier tag overrides config.model.

- [x] Add an MIT `LICENSE` file at the repo root. Use the standard, unmodified MIT License text with the copyright line `Copyright (c) 2026 mreinhofferxd-pixel`. Nothing else in the file. Commit only the LICENSE file.
- [x] Create `README.md` at the repo root for the public launch (none exists today) — modern GitHub style, informative and terse, zero filler, no fake claims. Structure top to bottom: (1) `# nocturne` heading, then STATIC shields.io badges only (the repo is private until launch, so no CI badge and no dynamic repo-linked badges; use the `https://img.shields.io/badge/<label>-<message>-<color>` form): python `3.10+`, license `MIT`, tests `323 passing`, platform `Windows | Linux | macOS`. (2) A one-line pitch: nocturne is a Claude Code skill that turns a repo plus a checkbox backlog into a self-verifying autonomous dev loop — a design-layer Claude reads the repo, authors a tailored harness, and launches a headless `claude -p` while-loop that implements, verifies, and commits each task on a dedicated branch. Directly under it, the tagline in italics: *a task exists in a done/not-done superposition until the harness observes it.* (3) A "Quick start" section readable in 30 seconds, nothing to install: prerequisites are the Claude Code CLI, `python` 3.10+ on PATH, and a git repo with either a `- [ ]` checkbox `BACKLOG.md` or a spec/design doc to groom; then two steps — copy `.claude/skills/nocturne/` into the target repo's `.claude/skills/`, open Claude Code there and run `/nocturne`; the skill previews the backlog, gate, branch, and caps, and waits for an explicit GO before launching. (4) A "What it does" bullet list, one line each: quality gate auto-derived from the repo (an explicit "Done = <cmd>" statement outranks CI config, package scripts, and language defaults) and independently re-run by the harness; trust-but-verify — done requires the gate green AND a real new commit AND a clean worktree, self-report is never trusted; atomic commits — one commit per task, failed attempts fully discarded; rate-limit pause-resume — a 429 never burns a retry, the loop sleeps to the reset and re-runs the same attempt; per-task model tiering with automatic retry escalation; an anti-gaming diff guard that rejects green-but-test-suppressing attempts; acceptance criteria parsed from the backlog, enforced in the diff, non-codifiable ones routed to a human checkpoint; budget guardrails (dollar cap, wall-clock cap, per-task timeout, protected paths); observability — live in-terminal feed, `.loop/report.md` dashboard, a global `~/.nocturne` run registry with statusline and events-feed surfaces, and a detached background mode. (5) A one-line "Configuration" section pointing to the full `loop.config.json` schema in `.claude/skills/nocturne/SKILL.md` — do not duplicate the schema. (6) An honest "v1 scope & limits" section: local-only (commits stay on a local loop branch; no push or PR automation yet), markdown checkbox backlogs only (github/gitlab issue adapters not built), the harness requires python 3.10+ and the `claude` CLI, and the loop model runs with broad Bash access — use on trusted repos. Wording note: if the README mentions how blocked tasks are treated on later runs, phrase it as "passed over", and keep test-suppression vocabulary out of the prose entirely (the harness diff-guard rejects diffs that contain it).
- [ ] Fix stale public-facing references that predate the detach feature. In `.claude/skills/nocturne/SKILL.md`: (a) the "v1 scope" paragraph ends with a "Still out:" list that still names the detach lifecycle (§11) — the detach subcommand has shipped (step 7 documents it), so remove it from that list, leaving dependency-aware PRs (§8.3 consumer) and github/gitlab adapters (§17); (b) in "Notes / accepted risks (v1)" the bullet reading "Attached only: closing the session ends the loop. Detached/overnight is fast-follow." is now wrong — replace it with one line noting that `python .loop/orchestrator.py detach` runs the loop as an OS-detached background process that survives the session (details in step 7). In `tests/test_rate_limit.py`: the comment above `REJECT_LINE` cites a private session memory ("see loop-creator-v1-status memory") that means nothing to a public reader — reword the comment to say the two signal shapes were captured verbatim from a live org five-hour rejection, with no memory reference. Comment and docs edits only; behavior and tests unchanged.
